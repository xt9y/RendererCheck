# RendererCheck examples

- `basic-c`: deterministic committed-baseline regression fixture and custom metrics.
- `raylib`: real framebuffer capture with warm-up frames.
- `vulkan-validation`: real `VK_LAYER_KHRONOS_validation` pass/fail integration.

Every example uses the public headers and the `renderercheck` CLI.
