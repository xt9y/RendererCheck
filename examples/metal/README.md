# Metal

A macOS offscreen Metal render-target capture. No window or display server is required.

```bash
make
renderercheck run metal-frame
renderercheck approve metal-frame
renderercheck run metal-frame
```
