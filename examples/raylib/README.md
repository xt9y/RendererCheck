# raylib

Real framebuffer capture through raylib. RendererCheck asks the example to render warm-up frames before capturing.

```bash
make
renderercheck run raylib-frame
renderercheck approve raylib-frame
renderercheck run raylib-frame
```
