# SDL3

A deterministic SDL3 renderer capture using `SDL_RenderReadPixels` and the RendererCheck public capture helper.

```bash
make
renderercheck run sdl3-frame
renderercheck approve sdl3-frame
renderercheck run sdl3-frame
```
